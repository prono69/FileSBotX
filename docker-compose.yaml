version: "3.9"

services:
  filestream-run:
    build: .
    container_name: fsb
    restart: always
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - nginx-proxy_default

networks:
  nginx-proxy_default:
    external: true